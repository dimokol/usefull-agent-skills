{
  "_comment": "Claude Code: place at your project root as .mcp.json (the installer does this for you). Replace the two absolute paths.",
  "mcpServers": {
    "agent-chat": {
      "command": "node",
      "args": ["/absolute/path/to/mcp/agent-chat/server.mjs"],
      "env": {
        "AGENT_CHAT_DIR": "/absolute/path/to/your-project/.agent-chat"
      }
    }
  }
}
